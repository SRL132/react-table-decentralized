{
  "name": "react-table",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ react-table",
    "create-local": "graph create --node http://localhost:8020/ react-table",
    "remove-local": "graph remove --node http://localhost:8020/ react-table",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 react-table",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.65.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
